@page "/Session/{SessionId}"
@using ArcheryTracker.Logic
@using ArcheryTracker.Logic.Models

@inject NavigationManager NavigationManager
@inject SessionService SessionService

<h1>@_session.Date.ToString("MMMM dd") Session</h1>

<p>Total Shots: @_session.TotalShots</p>
<p>Best round score: @_session.BestRoundScore</p>
<p>Average round score: @_session.AverageRoundScore</p>

@if (_loading)
{
    <p><em>Loading...</em></p>
}
else
{
    if (!_loading && _session == null)
    {
        NavigationManager.NavigateTo("/NotFound");
    }
    
    @if (_session.RoundScores != null && _session.RoundScores.Count != 0)
    {
        <table class="table">
            <thead>
            <tr>
                <td>Round</td>
                <td>Shots</td>
                <td>Total Score</td>
                <td colspan="12">Arrow Scores</td>
            </tr>
            </thead>
            <tbody>
    
            @foreach (var round in _session.RoundScores)
            {
                _roundCounter++;
                <tr>
                    <td>@_roundCounter</td>
                    <td>@round.TotalShots</td>
                    <td>@round.TotalScore</td>
                    @foreach (var score in round.Scores)
                    {
                        <td>@score</td>
                    }
                </tr>
            }
            </tbody>
        </table>
    }
    else
    {
        <p>Looks like this session is empty.</p>
    }
    <a href=@_addRoundUri>Add scores <span class="oi oi-chevron-right"></span></a>

}

@code {
    [Parameter]
    public string SessionId { get; set; }
    private Session _session { get; set; }
    private int _roundCounter { get; set; }
    private string _addRoundUri;
    private bool _loading = true;

    protected override async Task OnInitializedAsync()
    {
        _roundCounter = 0;
        if (SessionId != null)
        {
            _session = await SessionService.GetSession(SessionId);
            _addRoundUri = $"/Session/{_session.Id}/AddRound";
            _loading = false;
        }
    }
}